The data is gathered directly from Netflix Prize data, and the website is as follow:
https://www.kaggle.com/netflix-inc/netflix-prize-data

After unzipping the files, to successsfully run the matrix factorization models, run the preprocessor.ipynb and shrink_rating.ipynb first
for data preprocessing.

To run the autoencoder model, run 2sparse.ipynb as well before running the model.
